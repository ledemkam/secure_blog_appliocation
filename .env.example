# Fichier de compatibilite optionnel.
# Convention recommandee: utiliser `.env.dev` et `.env.prod`
# a partir de `.env.dev.example` et `.env.prod.example`.

# Profil actif par defaut si vous utilisez ce fichier unique.
SPRING_PROFILES_ACTIVE=dev


